import 'package:flutter/material.dart';

class ManuaisScreen extends StatelessWidget {
  const ManuaisScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Manuais')));
}
