import 'package:flutter/material.dart';

class ContratoScreen extends StatelessWidget {
  const ContratoScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Contrato')));
}
