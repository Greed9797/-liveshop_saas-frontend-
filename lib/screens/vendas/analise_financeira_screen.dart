import 'package:flutter/material.dart';

class AnaliseFinanceiraScreen extends StatelessWidget {
  const AnaliseFinanceiraScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Análise Financeira')));
}
