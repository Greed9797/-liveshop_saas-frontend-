import 'package:flutter/material.dart';

class ExcelenciaScreen extends StatelessWidget {
  const ExcelenciaScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Excelência')));
}
