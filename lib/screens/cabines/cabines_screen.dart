import 'package:flutter/material.dart';

class CabinesScreen extends StatelessWidget {
  const CabinesScreen({super.key});

  @override
  Widget build(BuildContext context) =>
      const Scaffold(body: Center(child: Text('Cabines')));
}
