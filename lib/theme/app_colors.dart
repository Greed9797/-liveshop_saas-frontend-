import 'package:flutter/material.dart';

class AppColors {
  // Cores principais da referência (EMIVE Style)
  static const Color primary = Color(0xFFFFC107); // Amarelo/Amber
  static const Color headerBlack = Color(0xFF000000); // Topo Preto
  static const Color background =
      Color(0xFFD1D5DB); // Fundo cinza médio das fotos
  static const Color surface = Color(0xFFF3F4F6); // Fundo dos cards

  static const Color textPrimary = Color(0xFF111827);
  static const Color textSecondary = Color(0xFF4B5563);

  // Status e Alertas
  static const Color success = Color(0xFF10B981);
  static const Color danger = Color(0xFFEF4444);
  static const Color warning = Color(0xFFF59E0B);
  static const Color info = Color(0xFF3B82F6);
  static const Color lilac = Color(0xFF8B5CF6);

  // Compatibilidade
  static const Color bgLight = Color(0xFFF8FAFC);
}
