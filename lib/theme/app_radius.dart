/// Border radius tokens for consistent visual hierarchy
class AppRadius {
  static const double xs   = 4.0;   // progress bars, tiny elements
  static const double sm   = 6.0;   // small containers, badges
  static const double badge = 6.0;  // status badges
  static const double md   = 8.0;   // buttons, chips, menu items
  static const double chip = 20.0;  // filter chips (pill-like)
  static const double lg   = 16.0;  // cards — premium feel
  static const double xl   = 16.0;  // modals, dialogs
  static const double pill = 999.0; // full pill shape
}
